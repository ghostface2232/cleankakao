// Custom WinUI3-style theme for iced widgets.

#[derive(Debug, Clone, Copy, Default)]
pub struct WinUi3Theme {
    // TODO: palette tokens (accent, surface, stroke, text).
}

impl WinUi3Theme {
    pub fn light() -> Self {
        Self::default()
    }

    pub fn dark() -> Self {
        Self::default()
    }
}
